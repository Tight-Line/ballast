apiVersion: ballast.tightlinesoftware.com/v1
kind: ClusterResourcePolicy
metadata:
  labels:
    app.kubernetes.io/name: ballast
    app.kubernetes.io/managed-by: kustomize
  name: clusterresourcepolicy-sample
spec:
  # TODO(user): Add fields here
