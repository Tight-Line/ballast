---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: metricssources.ballast.tightlinesoftware.com
spec:
  group: ballast.tightlinesoftware.com
  names:
    kind: MetricsSource
    listKind: MetricsSourceList
    plural: metricssources
    singular: metricssource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.config.pollInterval
      name: PollInterval
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: MetricsSource is the Schema for the metricssources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MetricsSource
            properties:
              config:
                description: Config holds plugin-specific configuration.
                properties:
                  pollInterval:
                    default: 60s
                    description: PollInterval is the minimum time between metric collection
                      cycles.
                    type: string
                  reservoirSize:
                    default: 10000
                    description: ReservoirSize is the hard cap on Redis sorted-set
                      entries per container per metric.
                    format: int64
                    minimum: 1
                    type: integer
                type: object
              type:
                description: Type identifies the metrics plugin (e.g. "kubernetesMetrics").
                minLength: 1
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of MetricsSource
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
